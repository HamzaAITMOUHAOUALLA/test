name: Cron Début de Journée

on:
  schedule:
    - cron: '38 1 * * *' # Tous les jours à 00:00 UTC

jobs:
  job_debut_jour:
    runs-on: ubuntu-latest
    steps:
      - name: Afficher le message
        run: echo "Début d'une nouvelle journée - Job exécuté"
